## 카테고리

- 자료구조 / 스택

## 문제

- 10828 / 스택
- [URL](https://www.acmicpc.net/problem/10828)

## 성공 유무

- 입력(성공)

## 작성 코드

```cpp
#include <iostream>
using namespace std;

int tos; //top of stack
int arr[10001]; //stack 배열 선언

void push(int x) 
	arr[tos] = x;
	pos++;
}

int pop() {
	if (tos == 0) return -1;
	int top = arr[tos - 1];
	tos--;
	return top;
}

int size() {
	return tos;
}

int top() { 
	if (tos == 0) return -1;
	return arr[tos - 1];
}

int empty() {
	if (tos == 0) return 1;
	else return 0;
}

int main() {
    
	int n;
	cin >> n;
	while (n--) {
		string str;
		cin >> str;
		if (str == "push") {
			int n;
			cin >> n;
			push(n);
		}
		else if (str == "pop") {
			cout << pop() << '\n';
		}
		else if (str == "top") {
			cout << top() << '\n';
		}
		else if (str == "size") {
			cout << size() << '\n';
		}
		else if (str == "empty") {
			cout << empty() << '\n';
		}
	}
}
```

## 공유 내용

`실패 코드 혹은 공유 내용 입력`

- stack → 선입선출
- 메인은 풀이를 참고해서 작성했습니다.
